<div class="section">
  <%= simple_form_for @auteur do |f| %>
    <div class="field">
      <div class="control">
        <%= f.input :nom_prenom, input_html: { class: 'input' }, wrapper: false, label_html: { class: 'label' } %>
      </div>
    </div><br>
    <%= f.button :submit, class: "button is-primary" %>
  <% end %>
</div>